{
  "name": "karma-growl-notifications-reporter",
  "version": "0.0.1",
  "description": "Karma growl reporter that falls back to Notification Center support on OS X",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "growl": "~1.7.0"
  }
}
